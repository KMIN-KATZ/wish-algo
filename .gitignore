# =====================================================================
#  这个仓库会被公开（GitHub Pages 免费版要求 public repo），
#  所以下面这些**个人/本地**的东西绝不能提交进去。
#  Pages 只发布 docs/ 目录，但仓库本身是公开可浏览的，别指望"没发布就看不到"。
# =====================================================================

# 个人工作日志与自动化执行记录（含项目内部分析，属私事）
.workbuddy/memory/

# 运行时产物：服务端抓取缓存、临时验证目录
.workbuddy/cache/
.workbuddy/tmp/

# ★凭据★ 本地推送用的 GitHub PAT。仓库是公开的，这份文件绝不能传上去。
.workbuddy/.git-token

# 之前调试时抓下来的一份 /api/data 响应，没有保留价值
.workbuddy/api.json

# 常规
node_modules/
*.log
.DS_Store
Thumbs.db
__pycache__/
